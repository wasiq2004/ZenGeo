/**
 * Public contact details.
 *
 * One place, because these appear in the footer, both legal pages and the
 * contact block — and a phone number that is right in three of four spots is
 * worse than one that is wrong everywhere, since nobody goes looking for the
 * stale one.
 */
export const CONTACT = {
  email: 'aiwithsalman33@gmail.com',
  /** As shown to a reader. */
  phoneDisplay: '+91 81480 35472',
  /** For `tel:` — no spaces, with the country code, so it dials from a phone. */
  phoneHref: '+918148035472',
} as const
